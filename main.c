#include "shell.h"

/**
 * main - Entry point of the simple shell program
 *
 * This function serves as the entry point for the
 * simple shell.
 * It calls the shell_main function to execute th
 * main logic of the shell.
 *
 * Return: Always returns 0.
 */
int main(void)
{
return (shell_main());
}
